# Copy to `.env.local` and fill in (never commit `.env.local`).
# Used by Next.js (chat) and by `npm run ingest` (embeddings).

OPENAI_API_KEY=

# Optional overrides:
# OPENAI_CHAT_MODEL=gpt-4o-mini
# OLLAMA_URL=http://localhost:11434
# OLLAMA_CHAT_MODEL=llama3.2
